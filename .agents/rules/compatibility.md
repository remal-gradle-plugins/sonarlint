# Compatibility

- Min supported Java version: read from `gradle.properties` key `java-runtime.min-version`. All Java code must be compatible with this version.
- Min supported Gradle version: read from `gradle.properties` key `gradle-api.min-version`.
