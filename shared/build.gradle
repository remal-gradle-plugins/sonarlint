apply plugin: 'java-library'

dependencies {
    relocateClasses 'com.google.code.gson:gson:2.10'
}

configurations.excludeFromClassesRelocation.exclude(group: 'com.google.code.gson', module: 'gson')
