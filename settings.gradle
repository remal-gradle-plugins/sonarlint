plugins {
    id 'org.gradle.toolchains.foojay-resolver-convention' version '0.6.0'
}

rootProject.name = 'sonarlint'

include(
    ':cross-compile--common',
    ':cross-compile--sonarlint-8.11-lte',
)
